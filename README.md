#  Birthday Wisher with Countdown! 

**Deskripsi Singkat:**

Aplikasi web ini memungkinkan Anda membuat dan membagikan ucapan ulang tahun yang disertai hitung mundur (countdown) menuju hari spesial. Cukup masukkan nama dan tanggal ulang tahun, lalu bagikan link-nya ke teman atau keluarga.

- Dibangun dengan React dan React Router
- Fitur utama: countdown, pembuatan ucapan, dan link yang bisa dibagikan
- Mudah dijalankan secara lokal menggunakan `pnpm`/Node.js

---

## 🚀 Cara Menjalankan Website Secara Lokal

1. **Clone repository ini:**
   ```bash
   git clone https://github.com/[username]/Birthday.git
   cd Birthday
   ```
2. **Install dependencies:**
   ```bash
   pnpm install
   ```
3. **Jalankan server lokal:**
   ```bash
   pnpm start
   ```
4. **Akses di browser:**
   Buka `http://localhost:3000` untuk mulai menggunakan aplikasi.

---

## 🛠️ Teknologi yang Digunakan

- **React 18** — Library utama untuk membangun UI
- **React Router DOM 6** — Routing antar halaman
- **React Scripts 5** — Toolchain build & development (Create React App)
- **Testing Library** — Untuk pengujian komponen
- **pnpm** — Package manager
- **Node.js** — Runtime environment

---

## Getting started

To get It running locally:

- Clone this repo
- `npm install` to install all req'd dependencies
- `npm start` to start the local server (this project uses create-react-app)


